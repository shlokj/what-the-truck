{
  "name": "what-the-truck",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm build",
    "test": "cd frontend && npm test",
    "eject": "cd frontend && npm eject",
    "install": "cd frontend && npm install"
  },
  "dependencies": {
    "firebase": "^9.14.0",
    "react-scripts": "^5.0.1"
  }
}
