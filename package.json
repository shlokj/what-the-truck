{
  "name": "what-the-truck",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start && cd ../backend && npm run dev",
    "build": "cd frontend && npm build",
    "test": "cd frontend && npm test",
    "eject": "cd frontend && npm eject",
    "install": "cd frontend && npm install"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
